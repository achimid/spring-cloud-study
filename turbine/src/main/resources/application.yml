server:
  port: 8989

spring:
  application:
    name: turbine

turbine:
  aggregator:
    clusterConfig: web-application2
  appConfig: web-application2


eureka:
  client:
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/